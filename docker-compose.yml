services:
  backend:
    image: 313694530717.dkr.ecr.eu-west-2.amazonaws.com/obucon:${IMAGE_TAG:-latest}
    container_name: obucon-backend
    env_file:
      - .env.prod
    ports:
      - "8080:8080"
    restart: unless-stopped
